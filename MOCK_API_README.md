# Mock API Documentation - CaarbonRentCar

## نظرة عامة
تم تحويل المشروع بالكامل ليعمل بدون باك إند باستخدام Mock API. جميع استدعاءات API الآن تستخدم بيانات وهمية محلية.

## بيانات المستخدمين الافتراضية

يمكنك تسجيل الدخول باستخدام:

1. **البريد الإلكتروني:** `user@example.com`
   **كلمة المرور:** `123456`

2. **البريد الإلكتروني:** `test@test.com`
   **كلمة المرور:** `password`

## الميزات المتاحة

### ✅ المصادقة (Authentication)
- تسجيل الدخول (Login)
- التسجيل (Register)
- معلومات المستخدم (User Info)
- التحقق من OTP (OTP Verification)

### ✅ المنتجات (Products)
- عرض جميع المنتجات (Get All Products)
- تفاصيل المنتج (Product Details)
- إضافة منتج جديد (Add Product)
- منتجات المستخدم (User Products)
- نشر/أرشفة المنتجات (Publish/Archive Products)

### ✅ الفئات (Categories)
- عرض جميع الفئات (Get All Categories)

### ✅ السلة (Cart)
- عرض السلة (Get Cart)
- إضافة للسلة (Add to Cart)
- حذف من السلة (Remove from Cart)
- تحديث الكمية (Update Cart Quantity)

### ✅ المفضلة (Wishlist)
- عرض المفضلة (Get Wishlist)
- إضافة للمفضلة (Add to Wishlist)
- حذف من المفضلة (Remove from Wishlist)

### ✅ العلامات (Tags)
- عرض جميع العلامات (Get All Tags)

## الملفات المعدلة

### Mock API Service
- `src/API/mockAPI.js` - خدمة Mock API الرئيسية

### Actions المحدثة
جميع ملفات الـ actions في `src/Store/` تم تحديثها لاستخدام Mock API:
- `src/Store/Login/actLogin/actLogin.js`
- `src/Store/Register/actRegister/actRegister.js`
- `src/Store/productSlice/actProducts/actProducts.js`
- `src/Store/Catogries/actCategory/actCategory.js`
- `src/Store/ProductDetails/actProductDetails/actProductDetails.js`
- `src/Store/userInfo/actUserInfo/actUserInfo.js`
- `src/Store/Cart/actions/*.js`
- `src/Store/WhisList/actwishlist/*.js`
- `src/Store/myProducts/actUserProduct/*.js`
- `src/Store/AddProduct/actAddProduct/actAddProduct.js`
- `src/Store/OTPSlice/actOTP/actOTP.js`
- `src/Store/Tags/actTags/actTags.js`

### Routes المحدثة
- `src/Router/App.jsx` - تم تفعيل جميع الـ routes

### Configuration
- `src/API/axios.js` - تم إزالة baseURL

## كيفية الاستخدام

1. **تشغيل المشروع:**
   ```bash
   npm install
   npm run dev
   ```

2. **تسجيل الدخول:**
   - اذهب إلى `/User/login`
   - استخدم أحد حسابات المستخدمين الافتراضية أعلاه

3. **استكشاف الميزات:**
   - جميع الصفحات متاحة الآن
   - البيانات محفوظة في الذاكرة (Memory)
   - عند إعادة تحميل الصفحة، سيتم إعادة تعيين البيانات (ما عدا token المحفوظ في localStorage)

## ملاحظات مهمة

⚠️ **البيانات مؤقتة:**
- جميع البيانات محفوظة في الذاكرة فقط
- عند إعادة تحميل الصفحة، سيتم فقدان البيانات (ما عدا token)
- البيانات لا تُحفظ في قاعدة بيانات حقيقية

✅ **الميزات:**
- جميع الـ API calls تعمل بشكل طبيعي
- التأخير المحاكي (500ms) لتحقيق تجربة واقعية
- معالجة الأخطاء متوفرة
- المصادقة تعمل بشكل كامل

## العودة للباك إند الحقيقي

إذا أردت العودة لاستخدام الباك إند الحقيقي:

1. أزل التعليق من `src/API/axios.js`:
   ```javascript
   axios.defaults.baseURL = "https://92.113.27.167:7644/";
   ```

2. استبدل جميع استدعاءات `mockAPI` بـ `axios` في ملفات الـ actions

3. تأكد من أن الباك إند يعمل ويستجيب بشكل صحيح

---

تم التحويل بنجاح! 🎉

